export const metadata = {
    title: 'Monitor — SimCardio',
    description: 'Monitor de signos vitales en tiempo real',
};

export default function MonitorLayout({ children }) {
    return children;
}
